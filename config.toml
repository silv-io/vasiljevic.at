# The URL the site will be built for
base_url = "https://vasiljevic.at"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

# Select Theme
theme = "hallo"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
greeting = "Hi there! 👋"

iam = "I'm"

links = [
    { title = "E-Mail", url = "mailto:s@vasiljevic.at", iconset = "fas", icon = "envelope" },
    { title = "GitHub", url = "https://github.com/silv-io", icon = "github" },
    { title = "Twitter", url = "https://twitter.com/@silv__io", icon = "twitter" }
]

[extra.author]
name = "Silvio"

[extra.theme]
background = "#FFF"
foreground = "#000"
hover = "#5a208a"
